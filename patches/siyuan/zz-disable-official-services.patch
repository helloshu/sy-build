diff --git a/kernel/mobile/kernel.go b/kernel/mobile/kernel.go
index 415ca10..b1691f0 100644
--- a/kernel/mobile/kernel.go
+++ b/kernel/mobile/kernel.go
@@ -129,6 +129,10 @@ func GetExportFileName(exportPath string) string {
 // -8：校验 transaction 失败
 // -9：未知的商品
 func VerifyAppStoreTransaction(accountToken, transactionID string) (retCode int) {
+	if util.OfficialCloudServicesDisabled {
+		return -2
+	}
+
 	retCode = -2
 	retMsg := "unknown error"
 
diff --git a/kernel/model/bazaar_rating.go b/kernel/model/bazaar_rating.go
index 5c44240..4635538 100644
--- a/kernel/model/bazaar_rating.go
+++ b/kernel/model/bazaar_rating.go
@@ -249,6 +249,9 @@ func validateBazaarPackageRatingRequest0(ctx context.Context, pkgType, packageNa
 }
 
 func getBazaarRatingUserToken() (string, error) {
+	if officialCloudServicesDisabled {
+		return "", ErrOfficialCloudServicesDisabled
+	}
 	user := Conf.GetUser()
 	if nil == user || "" == user.UserToken {
 		return "", errors.New(Conf.Language(31))
diff --git a/kernel/model/cloud_service.go b/kernel/model/cloud_service.go
index 5762a50..75e7757 100644
--- a/kernel/model/cloud_service.go
+++ b/kernel/model/cloud_service.go
@@ -37,9 +37,19 @@ import (
 	"github.com/siyuan-note/siyuan/kernel/util"
 )
 
-var ErrFailedToConnectCloudServer = errors.New("failed to connect cloud server")
+var (
+	ErrFailedToConnectCloudServer		= errors.New("failed to connect cloud server")
+	ErrOfficialCloudServicesDisabled	= errors.New("official cloud and account services are disabled")
+)
+
+const officialCloudServicesDisabled = util.OfficialCloudServicesDisabled
 
 func CloudChatGPT(msg string, contextMsgs []string) (ret string, stop bool, err error) {
+	if officialCloudServicesDisabled {
+		stop = true
+		err = ErrOfficialCloudServicesDisabled
+		return
+	}
 	user := Conf.GetUser()
 	if nil == user {
 		return
@@ -109,6 +119,9 @@ func CloudChatGPT(msg string, contextMsgs []string) (ret string, stop bool, err
 }
 
 func StartFreeTrial() (err error) {
+	if officialCloudServicesDisabled {
+		return ErrOfficialCloudServicesDisabled
+	}
 	user := Conf.GetUser()
 	if user == nil {
 		return errors.New(Conf.Language(31))
@@ -142,6 +155,9 @@ func StartFreeTrial() (err error) {
 }
 
 func DeactivateUser() (err error) {
+	if officialCloudServicesDisabled {
+		return ErrOfficialCloudServicesDisabled
+	}
 	release := lockAssetSourceChange()
 	defer release()
 	if Conf.Sync.Provider == conf.ProviderSiYuan {
@@ -183,6 +199,9 @@ func DeactivateUser() (err error) {
 }
 
 func SetCloudBlockReminder(id, data string, timed int64) (err error) {
+	if officialCloudServicesDisabled {
+		return ErrOfficialCloudServicesDisabled
+	}
 	user := Conf.GetUser()
 	if user == nil {
 		return errors.New(Conf.Language(31))
@@ -222,6 +241,9 @@ var uploadToken = ""
 var uploadTokenTime int64
 
 func LoadUploadToken() (err error) {
+	if officialCloudServicesDisabled {
+		return ErrOfficialCloudServicesDisabled
+	}
 	now := time.Now().Unix()
 	if 3600 >= now-uploadTokenTime {
 		return
@@ -269,8 +291,6 @@ var (
 )
 
 func RefreshCheckJob2H() {
-	go refreshSubscriptionExpirationRemind()
-	go refreshUser()
 	go refreshAnnouncement()
 }
 
@@ -395,6 +415,9 @@ func refreshAnnouncement() {
 }
 
 func RefreshUser(token string) (ret *conf.User, err error) {
+	if officialCloudServicesDisabled {
+		return Conf.GetUser(), nil
+	}
 	release := lockAssetSourceChange()
 	defer release()
 	previousUserID := ""
@@ -545,6 +568,9 @@ func loadUserFromConf() *conf.User {
 }
 
 func RemoveCloudShorthands(ids []string) (err error) {
+	if officialCloudServicesDisabled {
+		return ErrOfficialCloudServicesDisabled
+	}
 	user := Conf.GetUser()
 	if user == nil {
 		return errors.New(Conf.Language(31))
@@ -586,6 +612,10 @@ func RemoveCloudShorthands(ids []string) (err error) {
 }
 
 func GetCloudShorthand(id string) (ret map[string]any, err error) {
+	if officialCloudServicesDisabled {
+		err = ErrOfficialCloudServicesDisabled
+		return
+	}
 	user := Conf.GetUser()
 	if user == nil {
 		return nil, errors.New(Conf.Language(31))
@@ -637,6 +667,10 @@ func GetCloudShorthand(id string) (ret map[string]any, err error) {
 }
 
 func GetCloudShorthands(page int) (result map[string]any, err error) {
+	if officialCloudServicesDisabled {
+		err = ErrOfficialCloudServicesDisabled
+		return
+	}
 	user := Conf.GetUser()
 	if user == nil {
 		return nil, errors.New(Conf.Language(31))
@@ -707,6 +741,9 @@ var (
 )
 
 func getUser(token string) (*conf.User, error) {
+	if officialCloudServicesDisabled {
+		return nil, ErrOfficialCloudServicesDisabled
+	}
 	result := map[string]any{}
 	request := httpclient.NewCloudRequest30s().SetRetryCount(0)
 	resp, err := request.
@@ -745,6 +782,9 @@ func getUser(token string) (*conf.User, error) {
 }
 
 func UseActivationcode(code string) (err error) {
+	if officialCloudServicesDisabled {
+		return ErrOfficialCloudServicesDisabled
+	}
 	code = util.RemoveInvalid(code)
 	code = strings.TrimSpace(code)
 	if "" == code {
@@ -781,6 +821,9 @@ func UseActivationcode(code string) (err error) {
 }
 
 func CheckActivationcode(code string) (retCode int, msg string) {
+	if officialCloudServicesDisabled {
+		return 1, ErrOfficialCloudServicesDisabled.Error()
+	}
 	code = util.RemoveInvalid(code)
 	code = strings.TrimSpace(code)
 	if "" == code {
@@ -823,6 +866,9 @@ func CheckActivationcode(code string) (retCode int, msg string) {
 }
 
 func Login(userName, password, captcha string, cloudRegion int) (ret *gulu.Result) {
+	if officialCloudServicesDisabled {
+		return officialCloudDisabledResult()
+	}
 	release := lockAssetSourceChange()
 	defer release()
 	previousCloudRegion := util.CurrentCloudRegion
@@ -881,6 +927,9 @@ func Login(userName, password, captcha string, cloudRegion int) (ret *gulu.Resul
 }
 
 func Login2fa(token, code string) (ret *gulu.Result) {
+	if officialCloudServicesDisabled {
+		return officialCloudDisabledResult()
+	}
 	result := map[string]any{}
 	// 两步验证登录不是幂等操作，关闭自动重试以避免重复登录。
 	request := httpclient.NewCloudRequest30s().SetRetryCount(0)
@@ -913,6 +962,13 @@ func failedLogin2faResult() *gulu.Result {
 	return ret
 }
 
+func officialCloudDisabledResult() *gulu.Result {
+	ret := gulu.Ret.NewResult()
+	ret.Code = -1
+	ret.Msg = ErrOfficialCloudServicesDisabled.Error()
+	return ret
+}
+
 func parseLogin2faResult(result map[string]any) (ret *gulu.Result, ok bool) {
 	code, codeOK := result["code"].(float64)
 	msg, msgOK := result["msg"].(string)
diff --git a/kernel/model/export.go b/kernel/model/export.go
index 60ed55a..7ccb9c4 100644
--- a/kernel/model/export.go
+++ b/kernel/model/export.go
@@ -407,6 +407,10 @@ func ExportAv2CSV(avID, blockID string) (zipPath string, err error) {
 }
 
 func Export2Liandi(id string) (err error) {
+	if officialCloudServicesDisabled {
+		return ErrOfficialCloudServicesDisabled
+	}
+
 	if err = prepareExportBlockAssets(id, false); err != nil {
 		return
 	}
diff --git a/kernel/model/repository.go b/kernel/model/repository.go
index 353fca0..2bcfd13 100644
--- a/kernel/model/repository.go
+++ b/kernel/model/repository.go
@@ -1453,6 +1453,9 @@ func appendAgentRollbackEntries() {
 func DownloadCloudSnapshot(tag, id string) (err error) {
 	assetDownloadSourceMu.RLock()
 	defer assetDownloadSourceMu.RUnlock()
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		return ErrOfficialCloudServicesDisabled
+	}
 	if 1 > len(Conf.Repo.Key) {
 		err = errors.New(Conf.Language(26))
 		return
@@ -1499,6 +1502,9 @@ func DownloadCloudSnapshot(tag, id string) (err error) {
 func UploadCloudSnapshot(tag, id string) (err error) {
 	assetDownloadSourceMu.RLock()
 	defer assetDownloadSourceMu.RUnlock()
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		return ErrOfficialCloudServicesDisabled
+	}
 	if 1 > len(Conf.Repo.Key) {
 		err = errors.New(Conf.Language(26))
 		return
@@ -1545,6 +1551,9 @@ func UploadCloudSnapshot(tag, id string) (err error) {
 func RemoveCloudRepoTag(tag string) (err error) {
 	assetDownloadSourceMu.RLock()
 	defer assetDownloadSourceMu.RUnlock()
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		return ErrOfficialCloudServicesDisabled
+	}
 	if 1 > len(Conf.Repo.Key) {
 		err = errors.New(Conf.Language(26))
 		return
@@ -1581,6 +1590,10 @@ func GetCloudRepoTagSnapshots() (ret []*dejavu.Log, err error) {
 	assetDownloadSourceMu.RLock()
 	defer assetDownloadSourceMu.RUnlock()
 	ret = []*dejavu.Log{}
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		err = ErrOfficialCloudServicesDisabled
+		return
+	}
 	if 1 > len(Conf.Repo.Key) {
 		err = errors.New(Conf.Language(26))
 		return
@@ -1621,6 +1634,10 @@ func GetCloudRepoSnapshots(page int) (ret []*dejavu.Log, pageCount, totalCount i
 	assetDownloadSourceMu.RLock()
 	defer assetDownloadSourceMu.RUnlock()
 	ret = []*dejavu.Log{}
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		err = ErrOfficialCloudServicesDisabled
+		return
+	}
 	if 1 > len(Conf.Repo.Key) {
 		err = errors.New(Conf.Language(26))
 		return
@@ -3308,6 +3325,9 @@ func GetCloudSpace() (s *Sync, b *Backup, hSize, hAssetSize, hTotalSize, hExchan
 func getCloudSpace() (stat *cloud.Stat, err error) {
 	assetDownloadSourceMu.RLock()
 	defer assetDownloadSourceMu.RUnlock()
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		return nil, ErrOfficialCloudServicesDisabled
+	}
 	handleCloudError := cloudRepoErrorHandler()
 	defer func() { handleCloudError(err) }()
 	repo, err := newRepositoryWithAssetSourceLocked()
diff --git a/kernel/model/sync.go b/kernel/model/sync.go
index 190ce75..cfad2b5 100644
--- a/kernel/model/sync.go
+++ b/kernel/model/sync.go
@@ -429,6 +429,12 @@ func checkSync(boot, exit, byHand bool) bool {
 
 	switch Conf.Sync.Provider {
 	case conf.ProviderSiYuan:
+		if officialCloudServicesDisabled {
+			if byHand {
+				logging.LogWarnf("[sync-diag] checkSync skipped [reason=official-cloud-disabled, provider=%d]", Conf.Sync.Provider)
+			}
+			return false
+		}
 		if !IsSubscriber() {
 			if byHand {
 				logging.LogWarnf("[sync-diag] checkSync skipped [reason=subscription-gate, provider=%d]", Conf.Sync.Provider)
@@ -652,6 +658,9 @@ func SetSyncMode(mode int) {
 }
 
 func SetSyncProvider(provider int) (err error) {
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == provider {
+		return ErrOfficialCloudServicesDisabled
+	}
 	release := lockAssetSourceChange()
 	defer release()
 	if provider != Conf.Sync.Provider {
@@ -779,6 +788,9 @@ var (
 func CreateCloudSyncDir(name string) (err error) {
 	assetDownloadSourceMu.RLock()
 	defer assetDownloadSourceMu.RUnlock()
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		return ErrOfficialCloudServicesDisabled
+	}
 	switch Conf.Sync.Provider {
 	case conf.ProviderSiYuan, conf.ProviderLocal:
 		break
@@ -811,6 +823,9 @@ func CreateCloudSyncDir(name string) (err error) {
 func RemoveCloudSyncDir(name string) (err error) {
 	release := lockAssetSourceChange()
 	defer release()
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		return ErrOfficialCloudServicesDisabled
+	}
 	if name == Conf.Sync.CloudName {
 		if err = requireCompleteAssetDownloads(); err != nil {
 			return
@@ -854,6 +869,10 @@ func ListCloudSyncDir() (syncDirs []*Sync, hSize string, err error) {
 	assetDownloadSourceMu.RLock()
 	defer assetDownloadSourceMu.RUnlock()
 	syncDirs = []*Sync{}
+	if officialCloudServicesDisabled && conf.ProviderSiYuan == Conf.Sync.Provider {
+		err = ErrOfficialCloudServicesDisabled
+		return
+	}
 	var dirs []*cloud.Repo
 	var size int64
 
@@ -1124,6 +1143,9 @@ func closeSyncWebSocket() {
 func connectSyncWebSocket() {
 	defer logging.Recover()
 
+	if officialCloudServicesDisabled {
+		return
+	}
 	if !Conf.Sync.Enabled || !IsSubscriber() || conf.ProviderSiYuan != Conf.Sync.Provider {
 		return
 	}
diff --git a/kernel/util/rhy.go b/kernel/util/rhy.go
index 73fbe78..4313797 100644
--- a/kernel/util/rhy.go
+++ b/kernel/util/rhy.go
@@ -27,6 +27,8 @@ import (
 	"golang.org/x/sync/singleflight"
 )
 
+const OfficialCloudServicesDisabled = true
+
 var (
 	RhyCacheDuration = int64(3600 * 6)
 
@@ -51,6 +53,10 @@ func RefreshRhyResultJob() {
 }
 
 func GetRhyResult(ctx context.Context, force bool) (map[string]any, error) {
+	if OfficialCloudServicesDisabled {
+		return map[string]any{}, nil
+	}
+
 	if ContainerDocker == Container {
 		RhyCacheDuration = int64(3600 * 24)
 	}
