diff --git a/app/build.gradle b/app/build.gradle
index 3eee686..835b98e 100644
--- a/app/build.gradle
+++ b/app/build.gradle
@@ -5,7 +5,7 @@ plugins {
 
 // 多渠道打包配置
 apply from: rootProject.file('flavors.gradle')
-apply from: rootProject.file('signings.gradle')
+//apply from: rootProject.file('signings.gradle')
 apply from: rootProject.file('buildRelease.gradle')
 
 // 1. 配置 App Bundle (.aab) 的基础文件名
@@ -46,6 +46,23 @@ android {
         versionName siyuanVersionName
     }
 
+    signingConfigs {
+        release {
+            def ksFile = file("${rootDir}/keystore/siyuan.jks")
+            if (ksFile.exists()) {
+                storeFile ksFile
+                storePassword "123456"
+                keyAlias "siyuan"
+                keyPassword "123456"
+            } else {
+                storeFile ksFile
+                storePassword "123456"
+                keyAlias "siyuan"
+                keyPassword "123456"
+            }
+        }
+    }
+
     buildFeatures {
         buildConfig true
     }
@@ -53,7 +70,7 @@ android {
     buildTypes {
         release {
             // 正式版打包时指定签名证书
-            signingConfig signingConfigs.siyuanConfig
+            signingConfig signingConfigs.release
 
             minifyEnabled true
             shrinkResources true
